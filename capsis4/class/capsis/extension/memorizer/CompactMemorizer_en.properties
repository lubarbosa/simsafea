# Sample ResourceBundle properties file

CompactMemorizer.name = Compact memorizer
CompactMemorizer.description = During the simulation, memorizes only the root step and the last step calculated, the intermediate steps are deleted (memory saving)